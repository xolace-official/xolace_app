# What burnout is

Dev fixture body. Burnout is what long stretches of pressure without enough
recovery can feel like: tired in a way rest doesn't seem to reach, and
further from the things that used to matter.

This explainer ships `active: false` — it goes live only after
`library/admin:markSafetyReviewed` and a re-ingest with `active: true`.
