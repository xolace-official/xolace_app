import { ReflectScreen } from '@/src/components/reflect/reflect-screen';


/**
 * Render the protected route index page by displaying the ReflectScreen component.
 *
 * @returns A JSX element that renders the ReflectScreen component.
 */

export default function ProtectedIndex() {
  return <ReflectScreen />;
}
